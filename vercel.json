{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma generate && prisma migrate deploy && npm run seed && next build",
  "devCommand": "next dev -p 3010",
  "installCommand": "npm install",
  "regions": ["bom1"],
  "crons": [
    {
      "path": "/api/cron/run",
      "schedule": "0 6 * * *"
    }
  ]
}
